apiVersion: v1
kind: Service
metadata:
  name: {{ include "syntaxis.fullname" . }}
  labels:
    {{- include "syntaxis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syntaxis.selectorLabels" . | nindent 4 }}
